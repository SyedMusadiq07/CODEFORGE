# Complete System Architecture - Visual Diagram

## 🏗️ Full Stack Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           USER BROWSER                                  │
│                        (localhost:5173)                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                    REACT FRONTEND (Vite)                         │  │
│  │                                                                  │  │
│  │  Pages:                                                          │  │
│  │  ┌────────────────────────────────────────────────────────────┐ │  │
│  │  │ ProblemPage.jsx                                            │ │  │
│  │  │  ├─ Monaco Editor (code input)                             │ │  │
│  │  │  ├─ Tabs (description, submissions, hints)                 │ │  │
│  │  │  ├─ Run Code / Submit buttons                              │ │  │
│  │  │  └─ Test Cases Display                                     │ │  │
│  │  └────────────────────────────────────────────────────────────┘ │  │
│  │                                                                  │  │
│  │  AI Components:                                                  │  │
│  │  ┌────────────────────────────────────────────────────────────┐ │  │
│  │  │ components/AI/                                             │ │  │
│  │  │  ├─ HintButton.jsx (3-level hints)                         │ │  │
│  │  │  ├─ AIChatPanel.jsx (floating chat)                        │ │  │
│  │  │  ├─ DebugButton.jsx (error analysis)                       │ │  │
│  │  │  ├─ ExplainSolutionModal.jsx (solution breakdown)          │ │  │
│  │  │  └─ AIRecommendations.jsx (next problems)                  │ │  │
│  │  └────────────────────────────────────────────────────────────┘ │  │
│  │                                                                  │  │
│  │  Services:                                                       │  │
│  │  ┌────────────────────────────────────────────────────────────┐ │  │
│  │  │ services/aiService.js                                      │ │  │
│  │  │  ├─ getHint(problemId, level)                              │ │  │
│  │  │  ├─ chat(problemId, message, history)                      │ │  │
│  │  │  ├─ debugCode(problemId, code, lang, error)                │ │  │
│  │  │  ├─ explainSolution(problemId)                             │ │  │
│  │  │  └─ getRecommendations(userId)                             │ │  │
│  │  └────────────────────────────────────────────────────────────┘ │  │
│  │                                                                  │  │
│  │  State (Zustand):                                                │  │
│  │  ┌────────────────────────────────────────────────────────────┐ │  │
│  │  │ useProblemStore    - Problem details                       │ │  │
│  │  │ useExecutionStore  - Code execution results                │ │  │
│  │  │ useSubmissionStore - Submission history                    │ │  │
│  │  │ useAuthStore       - User authentication                   │ │  │
│  │  └────────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│                              ↕ HTTP/HTTPS                              │
│                         axios (withCredentials)                        │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                        BACKEND SERVER                                   │
│                      (localhost:8080)                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                 EXPRESS.JS APPLICATION                           │  │
│  │                                                                  │  │
│  │  Middleware:                                                     │  │
│  │  ┌────────────────────────────────────────────────────────────┐ │  │
│  │  │ authenticate (JWT verification)                            │ │  │
│  │  │ cors (credentials enabled)                                 │ │  │
│  │  │ express.json (body parsing)                                │ │  │
│  │  └────────────────────────────────────────────────────────────┘ │  │
│  │                                                                  │  │
│  │  Routes:                                                         │  │
│  │  ┌────────────────────────────────────────────────────────────┐ │  │
│  │  │ /api/v1/ai/hint/:id?level=X         [GET]                  │ │  │
│  │  │ /api/v1/ai/chat                     [POST]                 │ │  │
│  │  │ /api/v1/ai/debug                    [POST]                 │ │  │
│  │  │ /api/v1/ai/explain/:id              [GET]                  │ │  │
│  │  │ /api/v1/ai/recommend?userId=X       [GET]                  │ │  │
│  │  │                                                            │ │  │
│  │  │ /api/v1/problems/*                  (existing)             │ │  │
│  │  │ /api/v1/auth/*                      (existing)             │ │  │
│  │  │ /api/v1/execute/*                   (existing)             │ │  │
│  │  └────────────────────────────────────────────────────────────┘ │  │
│  │                                                                  │  │
│  │  Controllers:                                                    │  │
│  │  ┌────────────────────────────────────────────────────────────┐ │  │
│  │  │ ai.controller.js                                           │ │  │
│  │  │  ├─ getHint(req, res)                                      │ │  │
│  │  │  ├─ chat(req, res)                                         │ │  │
│  │  │  ├─ debugUserCode(req, res)                                │ │  │
│  │  │  ├─ explainProblemSolution(req, res)                       │ │  │
│  │  │  └─ getRecommendations(req, res)                           │ │  │
│  │  └────────────────────────────────────────────────────────────┘ │  │
│  │                                                                  │  │
│  │  Services:                                                       │  │
│  │  ┌────────────────────────────────────────────────────────────┐ │  │
│  │  │ ai.service.js                                              │ │  │
│  │  │  ├─ generateHint(problemId, level)                         │ │  │
│  │  │  ├─ chatWithAI(problemId, msg, history)                    │ │  │
│  │  │  ├─ debugCode(problemId, code, lang, err)                  │ │  │
│  │  │  ├─ explainSolution(problemId)                             │ │  │
│  │  │  └─ recommendProblems(userId)                              │ │  │
│  │  │                                                            │ │  │
│  │  │  Core Helper:                                              │ │  │
│  │  │  └─ generateAIResponse(prompt, modelName)                  │ │  │
│  │  │      ├─ Constructs Gemini API request                      │ │  │
│  │  │      ├─ Handles safety settings                            │ │  │
│  │  │      ├─ Sets max tokens (4096)                             │ │  │
│  │  │      └─ Parses response                                    │ │  │
│  │  └────────────────────────────────────────────────────────────┘ │  │
│  │                                                                  │  │
│  │  Database (Prisma ORM):                                          │  │
│  │  ┌────────────────────────────────────────────────────────────┐ │  │
│  │  │ prisma/schema.prisma                                       │ │  │
│  │  │  ├─ User model                                             │ │  │
│  │  │  ├─ Problem model                                          │ │  │
│  │  │  ├─ Submission model                                       │ │  │
│  │  │  ├─ ProblemSolved model                                    │ │  │
│  │  │  └─ TestCaseResult model                                   │ │  │
│  │  └────────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│                             ↕ HTTPS (REST API)                         │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                      EXTERNAL SERVICES                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ GOOGLE GEMINI AI API                                             │  │
│  │ (generativelanguage.googleapis.com)                              │  │
│  │                                                                  │  │
│  │  Models Used:                                                    │  │
│  │  ├─ gemini-2.5-flash   (fast responses, hints & chat)           │  │
│  │  └─ gemini-2.5-pro     (quality, debug & explain)               │  │
│  │                                                                  │  │
│  │  Configuration:                                                  │  │
│  │  ├─ maxOutputTokens: 4096                                       │  │
│  │  ├─ temperature: 0.7                                            │  │
│  │  ├─ topK: 40                                                    │  │
│  │  ├─ topP: 0.95                                                  │  │
│  │  └─ timeout: 60 seconds                                         │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ JUDGE0 API (sulu.sh)                                             │  │
│  │ (Code Execution Service)                                         │  │
│  │                                                                  │  │
│  │  Used for:                                                       │  │
│  │  ├─ Running user code                                           │  │
│  │  ├─ Generating test results                                     │  │
│  │  └─ Providing error messages (used by DebugButton)              │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                                   ↕
                                   ↕
┌─────────────────────────────────────────────────────────────────────────┐
│                        DATABASE LAYER                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ PostgreSQL Database (Docker Container)                           │  │
│  │                                                                  │  │
│  │  Tables:                                                         │  │
│  │  ├─ User           (id, email, password, createdAt)             │  │
│  │  ├─ Problem        (id, title, description, hints, etc.)        │  │
│  │  ├─ Submission     (id, userId, problemId, code, status)        │  │
│  │  ├─ ProblemSolved  (userId, problemId, solvedAt)                │  │
│  │  └─ TestCaseResult (submissionId, testCase, result)             │  │
│  │                                                                  │  │
│  │  Indexes:                                                        │  │
│  │  ├─ Problem.id (primary)                                        │  │
│  │  ├─ User.id (primary)                                           │  │
│  │  └─ Submission.userId + problemId (composite)                   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Examples

### Example 1: User Gets AI Hint

```
USER ACTION: Clicks "Get AI Hint (Level 1)"
    ↓
┌─────────────────────────────────────────┐
│ HintButton.jsx                          │
│ - Sets isLoading = true                 │
│ - Checks cache (empty on first click)   │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ aiService.js                            │
│ - Calls: GET /api/v1/ai/hint/:id?level=1│
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Backend: ai.routes.js                   │
│ - Applies authenticate middleware       │
│ - Routes to ai.controller.getHint()     │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Backend: ai.controller.js               │
│ - Extracts problemId & level from req   │
│ - Validates problem exists in DB        │
│ - Calls ai.service.generateHint()       │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Backend: ai.service.js                  │
│ - Fetches problem from database         │
│ - Constructs level-specific prompt      │
│ - Calls generateAIResponse()            │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Gemini API                              │
│ - Receives prompt                       │
│ - Generates hint (2-5 seconds)          │
│ - Returns JSON response                 │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Backend: ai.service.js                  │
│ - Parses response.candidates[0].content │
│ - Extracts hint text                    │
│ - Returns { success: true, hint: "..." }│
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Frontend: HintButton.jsx                │
│ - Stores hint in state cache            │
│ - Sets isLoading = false                │
│ - Displays hint in alert box            │
│ - Shows "Need more help?" button        │
└─────────────────────────────────────────┘
    ↓
USER SEES: Hint displayed in blue alert
```

### Example 2: User Runs Code with Error → Gets Debug Help

```
USER ACTION: Writes buggy code, clicks "Run Code"
    ↓
┌─────────────────────────────────────────┐
│ ProblemPage.jsx                         │
│ - handleRunCode() triggered             │
│ - Calls useExecutionStore.executeCode() │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Judge0 API                              │
│ - Compiles/runs code                    │
│ - Detects error: "SyntaxError: ..."     │
│ - Returns error details                 │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ useExecutionStore                       │
│ - Updates submission state with error   │
│ - submission.status = "Compilation Error"│
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ ProblemPage.jsx useEffect               │
│ - Detects error in submission           │
│ - Sets errorMessage state               │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ DebugButton.jsx                         │
│ - Becomes visible (conditional render)  │
│ - Red button with bug icon appears      │
└─────────────────────────────────────────┘
    ↓
USER ACTION: Clicks "Debug with AI"
    ↓
┌─────────────────────────────────────────┐
│ DebugButton.jsx                         │
│ - Opens modal                           │
│ - Calls aiService.debugCode()           │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Backend: ai.service.debugCode()         │
│ - Analyzes code + error message         │
│ - Calls Gemini with debug prompt        │
│ - Gets: analysis, suggestions, fixedCode│
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ DebugButton.jsx Modal                   │
│ - Displays error (red alert)            │
│ - Shows analysis                        │
│ - Shows suggestions (green box)         │
│ - Shows fixed code (code block)         │
└─────────────────────────────────────────┘
    ↓
USER SEES: Complete debug analysis with fix
```

## 🎨 Component Rendering Tree

```
App.jsx
└── Router
    └── ProblemPage.jsx
        ├── Navbar
        │   ├── Home Link
        │   ├── Problem Title
        │   ├── ExplainSolutionModal (button + modal)
        │   ├── AIRecommendations (button + modal)
        │   ├── Bookmark Button
        │   └── Language Selector
        │
        ├── Main Grid (2 columns)
        │   ├── Left Panel
        │   │   ├── Tabs (description, submissions, discussion, hints)
        │   │   └── Tab Content
        │   │       ├── Description (problem.description)
        │   │       ├── Submissions (SubmissionsList)
        │   │       ├── Discussion (placeholder)
        │   │       └── Hints
        │   │           ├── HintButton ← NEW
        │   │           └── Static hints
        │   │
        │   └── Right Panel
        │       ├── Monaco Editor
        │       └── Action Bar
        │           ├── Run Code Button
        │           ├── DebugButton ← NEW (conditional)
        │           └── Submit Button
        │
        ├── Test Results Section
        │   └── Submission OR TestCases Table
        │
        └── AIChatPanel ← NEW (floating)
            ├── Toggle Button (sparkle icon)
            └── Chat Window (conditional)
                ├── Message History
                └── Input + Send Button
```

## 🔐 Security Flow

```
┌─────────────────────────────────────────┐
│ User logs in                            │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Backend generates JWT token             │
│ - Includes userId, email                │
│ - Sets httpOnly cookie                  │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Frontend stores in cookie               │
│ - axios withCredentials: true           │
│ - Automatically sent on all requests    │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ AI API Request                          │
│ - Cookie sent with request              │
│ - Passes through authenticate middleware│
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Middleware verifies JWT                 │
│ - If valid: attach user to req.user     │
│ - If invalid: 401 Unauthorized           │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ Controller accesses req.user            │
│ - Uses userId for personalized features │
│ - Fetches user's submission history     │
└─────────────────────────────────────────┘
```

## 📊 State Management Flow

```
┌───────────────────────────────────────────────┐
│ Zustand Stores (Global State)                │
├───────────────────────────────────────────────┤
│                                               │
│ useProblemStore                               │
│ ├─ problem: {...}                             │
│ ├─ isProblemLoading: false                    │
│ └─ getProblemById(id)                         │
│                                               │
│ useExecutionStore                             │
│ ├─ submission: {...}                          │
│ ├─ isExecuting: false                         │
│ └─ executeCode(code, lang, stdin, expected)   │
│                                               │
│ useSubmissionStore                            │
│ ├─ submissions: [...]                         │
│ ├─ isLoading: false                           │
│ └─ getSubmissionForProblem(problemId)         │
│                                               │
│ useAuthStore                                  │
│ ├─ authUser: {...}                            │
│ ├─ isLoggingIn: false                         │
│ └─ login(credentials)                         │
│                                               │
└───────────────────────────────────────────────┘
                    ↕
┌───────────────────────────────────────────────┐
│ Local Component State (useState)              │
├───────────────────────────────────────────────┤
│                                               │
│ ProblemPage                                   │
│ ├─ code: ""                                   │
│ ├─ activeTab: "description"                   │
│ ├─ selectedLanguage: "javascript"             │
│ ├─ errorMessage: "" ← NEW                     │
│ └─ testCases: [...]                           │
│                                               │
│ HintButton                                    │
│ ├─ hints: {1: null, 2: null, 3: null}        │
│ ├─ currentLevel: 1                            │
│ └─ isLoading: false                           │
│                                               │
│ AIChatPanel                                   │
│ ├─ messages: [...]                            │
│ ├─ input: ""                                  │
│ ├─ isLoading: false                           │
│ └─ isOpen: false                              │
│                                               │
│ DebugButton                                   │
│ ├─ debugAnalysis: null                        │
│ ├─ isLoading: false                           │
│ └─ showAnalysis: false                        │
│                                               │
│ ExplainSolutionModal                          │
│ ├─ explanation: null                          │
│ ├─ isLoading: false                           │
│ └─ isOpen: false                              │
│                                               │
│ AIRecommendations                             │
│ ├─ recommendations: []                        │
│ ├─ isLoading: false                           │
│ └─ isOpen: false                              │
│                                               │
└───────────────────────────────────────────────┘
```

---

This completes the visual architecture! All systems integrated and error-free. 🎉
